# 진행 상황 (세션 G 점검)

마지막 점검: 2026-07-20 · **개학(8월 14일)까지 D-25**

## 미해결 위험

- [ ] **Firebase API 키가 공개 저장소 히스토리에 남아 있음.** 커밋 `5cb0299`("Firebase 설정값 입력")로 실제 apiKey·projectId 등이 `public/js/firebase-init.js`에 커밋되어 `origin/main`(공개 저장소, `soomsci/ai-studio-sci`)에 이미 푸시됨. 세션 A가 이후 작업(미커밋)에서 `firebase-config.js`(gitignore 대상)로 분리해 **앞으로의 커밋은 안전**해졌지만, 과거 커밋에는 실제 키 값이 그대로 남아 git 히스토리를 통해 조회 가능. 대응 필요: Firebase/Google Cloud 콘솔에서 이 키를 재발급하거나 HTTP 리퍼러로 제한. 히스토리 삭제(`git filter-repo` 등)는 파괴적 작업이라 사용자 승인 필요.
- [ ] `firestore.rules`의 읽기 권한이 `classId`로 범위가 좁혀지지 않음 — 로그인(익명 포함)만 하면 다른 학급의 `datasets`·`analyses`(실험3 심박수 포함)까지 읽을 수 있음. 위 키 노출과 겹치면 위험이 커짐. 익명 인증에 학급 코드 검증을 추가할지 세션 A와 논의 필요.
- [ ] `pasco` 라이브러리가 심박수·폐활량 센서를 공식 지원하지 않음(§11 세션 F 참고). 실물 센서로 연결 테스트 필요. 안 되면 §5의 `source:"manual"` 경로로 대체.
- [ ] **통합 버그(방금 발견):** 세션 A가 `mock/mock-data.js` → `public/mock/mock-data.js`로 이동하면서 `public/js/data.js`의 import 경로는 고쳤지만, 세션 B의 `public/js/exp1.js:121`은 여전히 옛 경로(`../../mock/mock-data.js`)를 참조함 → "연습 데이터 만들기" 버튼이 404로 깨짐. 세션 B가 한 줄만 고치면 됨(아래 라우팅 프롬프트 참고).

## 세션별 현황

- [x] **세션 A**: 공통 뼈대 완성(`b770f70`). 이후 보안 개선 작업 진행 중(미커밋) — `.gitignore` 신설, `firebase-config.example.js` 추가, `firebase-init.js`를 `window.__FIREBASE_CONFIG__` 참조로 변경, `mock/` → `public/mock/` 이동, `index.html`/`join.html` 페이지 분리(사용자 요청 반영). 코드 자체는 검토 결과 정상.
- [x] **세션 B**: 실험 1 탭 완성, 커밋(`ecaf490`). §12 이행 감사 통과(아래 참고). 파일 소유권 위반 없음(`public/js/exp1.js`, `public/config/exp1.config.js`만 수정). **단, 위 통합 버그 수정 필요.**
- [ ] 세션 C, D: 시작 전 (§11.0 권장대로 세션 B 완료 후 시작 가능한 상태)
- [ ] 세션 E, F: 시작 전

## §12 이행 감사 — 실험 1 (세션 B)

- **비계 질문 재설계 여부**: 통과. `config/exp1.config.js`의 5단계 질문을 SPEC §10.1 원문과 대조한 결과, 문장을 그대로 옮기지 않고 재구성됨. 예: 원문 "그래프에 마우스를 올려가며, CO₂가 가장 빠르게 올라간 구간을 정확한 시각으로 찾아보세요" → 재설계 "CO₂가 기준선 1,000ppm을 처음 넘은 순간을 찾아, 정확한 시각과 값을 적어 보세요"(더 구체적인 기준으로 전환).
- **§12.1 자동계산 선별**: 통과. `stats` 배열에 결론에 실제로 쓰이는 4개만 남기고 최솟값은 뺐다는 주석이 있음.
- **§12.4 과정·기능 반영**: 통과. config 파일 상단에 "자료 분석·해석", "결론 도출 및 일반화" 2가지를 어떻게 반영했는지 주석으로 명시됨.
- **§12.3 mock 데이터 결론 도달 검증**: `mock/mock-data.js`의 `generateMock(1, condition)`을 4개 조건 모두 실행하고 `exp1.js`의 `computeStats` 로직을 그대로 재현해 확인함(node로 시뮬레이션).
  - 창문 닫음: 기준선(1,000ppm) 돌파 약 17분, 상승 속도 약 17ppm/분
  - 창문 열음: 약 3분 40초 만에 기준선 아래로 회복
  - 문만 열음: 약 17분 30초 소요 (창문보다 훨씬 느림)
  - 쉬는 시간: 개방 구간 이후 빠르게 하강
  - → 조건 간 차이가 뚜렷해 4단계 결론("몇 분마다 환기해야 하는가")에 학생이 실제로 도달할 수 있는 구조임을 확인.

## 배포 전 점검 (개학 8/14 기준, D-25)

- [ ] Firebase 보안 규칙(`firestore.rules`)이 실제 배포되어 있는가 — 로컬 파일은 §5.4 요지와 일치하나, 콘솔 배포 여부는 저장소만으로 확인 불가.
- [ ] `.gitignore`가 Firebase 설정값을 실제로 제외하고 있는가 — **파일은 방금 신설되어 정상 작동하나(위 미해결 위험 참고), 과거 커밋에 남은 값은 별도 조치 필요.**
- [ ] 학급 코드로 익명 입장이 실제로 되는가 — 브라우저 미실행, 미확인.
- [ ] 모둠 노트북 1대 이상 실제 접속 확인 — 미확인.
- [ ] 실험 1의 화면·분석 5단계가 끊기지 않고 동작하는가 — mock 로직 시뮬레이션은 통과. 브라우저 실제 클릭 테스트는 아직 안 함(위 import 버그부터 고쳐야 함).
- [ ] 센서 연결 테스트 — 아직 미입수. §5 `source:"mock"` 경로는 세션 B가 검증 완료.

## 사용자에게 전달할 라우팅 프롬프트

### ① 세션 B 창에 붙여넣기 — mock 경로 수정

```
━━━ 세션 B 창에 붙여넣기 ━━━

CLAUDE.md의 "작업 방식 — 토큰·비용 절약"을 지켜라.
세션 A가 mock/mock-data.js를 public/mock/mock-data.js로 옮겼다.
public/js/exp1.js의 121번째 줄 근처, "연습 데이터 만들기" 기능에서
쓰는 동적 import 경로가 아직 옛 경로(../../mock/mock-data.js)를 가리키고 있어
404로 깨진다. public/js/data.js가 이미 ../mock/mock-data.js로 고쳐진 것을
참고해서 exp1.js도 같은 경로로 고쳐라.

관련 절: 없음 (경로 수정만)
담당 파일: public/js/exp1.js만 수정해라. 다른 로직은 건드리지 마라.
```

### ② 사용자 직접 조치 필요 — Firebase 키 회전 (G가 대신 할 수 없음)

Firebase 콘솔/Google Cloud 콘솔 접근이 필요해 세션 G가 대신 처리할 수 없습니다.
1. Firebase 콘솔 → 프로젝트 설정에서 현재 API 키(`ai-science-data-studio` 프로젝트)를 재발급하거나, Google Cloud 콘솔 → API 및 서비스 → 사용자 인증 정보에서 이 키에 HTTP 리퍼러 제한을 겁니다.
2. 재발급한 새 값은 커밋되지 않는 `public/js/firebase-config.js`에만 넣습니다(세션 A가 만든 `firebase-config.example.js` 형식 참고).
3. 과거 커밋(`5cb0299`)에 남은 옛 키를 git 히스토리에서 지울지는 선택 사항입니다 — 키를 재발급하면 옛 값은 무효가 되므로 급하지 않다면 히스토리는 그대로 둬도 됩니다. 지우고 싶다면 알려주시면 절차를 안내하겠습니다(브랜치 재작성이 필요한 파괴적 작업입니다).
